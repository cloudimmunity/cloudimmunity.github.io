<rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title>golang - Devs ♥ Security</title><description>Code, Security, and Everything in Between.</description><link>http://localhost:2368/</link><generator>Ghost 0.5</generator><lastBuildDate>Fri, 01 May 2015 02:27:36 GMT</lastBuildDate><atom:link href="http://localhost:2368/tag/golang/rss/" rel="self" type="application/rss+xml"/><ttl>60</ttl><item><title>Traps, Gotchas and Common Mistakes in Go (golang)</title><description>&lt;p&gt;Go is a simple language, but, like any other language, it has a few gotchas... Many of those gotchas are not entirely Go's fault. Some of these mistakes are natural traps if you are coming from another language. Others are due to faulty assumptions and missing details.&lt;/p&gt;

&lt;p&gt;A lot of these gotchas may seem obvious if you took the time to learn the language reading the official spec, wiki, mailing list discussions, many great posts and presentations by Rob Pike, and the source code. Not everybody starts the same way though and that's ok.&lt;/p&gt;

&lt;p&gt;The dependency management, immature library echosystem, garbage collection, and performance related gotchas are not included. They deserve their own posts :-)&lt;/p&gt;

&lt;h6 id="openingbracecantbeplacedonaseparateline"&gt;Opening Brace Can't Be Placed on a Separate Line&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;In most other languages that use braces you get to choose where you place them. Go is different. You can thank automatic semicolon injection (without lookahead) for this behavior. Yes, Go does have semicolons :-)&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main()  
{ //error, can't have the opening brace on a separate line
    fmt.Println("hello there!")
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Error:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox826898458/main.go:6: syntax error: unexpected semicolon or newline before {
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    fmt.Println("works!")
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="unusedvariables"&gt;Unused Variables&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;If you have an unused variable your code will fail to compile. There are a couple of gotchas to this gotcha though :-) &lt;/p&gt;

&lt;p&gt;This rule applies only to the variable declared inside a function. This means it's ok if you have an unused global variable.&lt;/p&gt;

&lt;p&gt;Assigning a new value to the unused variable (as a separate statement) doesn't satisfy the requirement, so your code will still fail to compile.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

var gvar int //not an error

func main() {  
    var one int   //error, unused variable
    two := 2      //error, unused variable
    var three int //error, even though it's assigned 3 on the next line
    three = 3     
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Errors:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox473116179/main.go:6: one declared and not used
  /tmp/sandbox473116179/main.go:7: two declared and not used
  /tmp/sandbox473116179/main.go:8: three declared and not used
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    var one int
    _ = one

    two := 2 
    fmt.Println(two)

    var three int 
    three = 3
    one = three
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Another option is to comment out or remove the unused variables :-)&lt;/p&gt;

&lt;h6 id="unusedimports"&gt;Unused Imports&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Your code will fail to compile if you import a package without using any of its exported functions, interfaces, structures or variables.&lt;/p&gt;

&lt;p&gt;If you really need the imported package you can use the blank identifier (underscore character) as its package name to avoid this compilation failure. The blank identifier is used to import packages for their side effects.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import (  
    "fmt"
    "log"
    "time"
)

func main() {  
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Errors:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox627475386/main.go:4: imported and not used: "fmt"
  /tmp/sandbox627475386/main.go:5: imported and not used: "log"
  /tmp/sandbox627475386/main.go:6: imported and not used: "time"
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import (  
    _ "fmt"
    "log"
    "time"
)

var _ = log.Println

func main() {  
    _ = time.Now
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Another option is to remove or comment out the unused imports :-)&lt;/p&gt;

&lt;h6 id="shortvariabledeclarationscanbeusedonlyinsidefunctions"&gt;Short Variable Declarations Can Be Used Only Inside Functions&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

myvar := 1 //error

func main() {  
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Error:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox265716165/main.go:3: non-declaration statement outside function body
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

var myvar = 1

func main() {  
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="redeclaringvariablesusingshortvariabledeclarations"&gt;Redeclaring Variables Using Short Variable Declarations&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;You can't redeclare a variable on its own. It's possible only in a multi-variable short declaration where one or more new variables are declared.&lt;/p&gt;

&lt;p&gt;The redeclared variable also has to be in the same block or you'll end up with a shadowed variable.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    one := 0
    one := 1 //error
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Error:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox706333626/main.go:5: no new variables on left side of :=
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    one := 0
    one, two := 1,2

    one,two = two,one
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="accidentalvariableshadowing"&gt;Accidental Variable Shadowing&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The short variable declaration syntax is so convinient (especially for those coming from a dynamic language) that it's easy to treat it like a regular assignment operation. The ability to redeclare variables also makes this mistake more likely.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := 1
    fmt.Println(x)     //prints 1
    {
        fmt.Println(x) //prints 1
        x := 2
        fmt.Println(x) //prints 2
    }
    fmt.Println(x)     //prints 1 (bad if you need 2)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="cantuseniltoinitializeavariablewithoutanexplicittype"&gt;Can't Use "nil" to Initialize a Variable Without An Explicit Type&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;"nil" can be used as the "zero value" for interfaces, functions, pointers, maps, slices, and channels. If you don't specify the variable type the compiler will fail to compile your code because it can't guess the type.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    var x = nil //error

    _ = x
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Error:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox188239583/main.go:4: use of untyped nil
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    var x interface{} = nil

    _ = x
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="usingnilslicesandmaps"&gt;Using "nil" Slices and Maps&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;It's ok to add items to a "nil" slice, but doing the same with a map will produce a runtime panic.&lt;/p&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    var s []int
    s = append(s,1)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    var m map[string]int
    m["one"] = 1 //error

}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="mapcapacity"&gt;Map Capacity&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;You can specify the map capacity when it's created, but you can't use the "cap" function on maps.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    m := make(map[string]int,99)
    cap(m) //error
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Error:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox326543983/main.go:5: invalid argument m (type map[string]int) for cap
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h6 id="stringscantbenil"&gt;Strings Can't Be "nil"&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This is a gotcha for developers who are used to assigning "nil" to string variables.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    var x string = nil //error

    if(x == nil) { //error
        x = "default"
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Errors:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox630560459/main.go:4: cannot use nil as type string in assignment
  /tmp/sandbox630560459/main.go:6: invalid operation: x == nil (mismatched types string and nil)
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    var x string //defaults to "" (zero value)

    if(x == "") {
        x = "default"
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="arraysarepassedbyvalue"&gt;Arrays Are Passed By Value&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := [3]int{1,2,3}

    func(arr [3]int) {
        arr[0] = 7
        fmt.Println(arr) //prints [7 2 3]
    }(x)

    fmt.Println(x) //prints [1 2 3] (not ok if you need [7 2 3])
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="unexpectedvaluesinsliceandarrayrangeclauses"&gt;Unexpected Values in Slice and Array "range" Clauses&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This can happen if you are used to the "for-in" or "foreach" statements in other languages. The "range" clause in Go is different. It generates two values: the first value is the item index while the second value is the item data.&lt;/p&gt;

&lt;p&gt;Bad:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := []string{"a","b","c"}

    for v := range x {
        fmt.Println(v) //prints 0, 1, 2
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Good:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := []string{"a","b","c"}

    for _, v := range x {
        fmt.Println(v) //prints a, b, c
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="slicesandarraysareonedimensional"&gt;Slices And Arrays Are One-Dimensional&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;It may seem like Go supports multi-dimensional arrays and slices, but it doesn't. Creating arrays of arrays or slices of slices is possible. Each inner-array/slice has to be initialized separately.&lt;/p&gt;

&lt;h6 id="accessingnonexistingmapkeys"&gt;Accessing Non-Existing Map Keys&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This is a gotcha for developers who expect to get "nil" values (like it's done in other languages). The returned value will be "nil" if the "zero value" for the corresponding data type is "nil", but it'll be different for other data types. Checking for the appropriate "zero value" can be used to determine if the map record exist, but it's not always reliable (e.g., what do you do if you have a map of booleans where the "zero value" is false). The most reliable way to know if the map record exist is to check the second value return by the map access operation.&lt;/p&gt;

&lt;p&gt;Bad:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := map[string]string{"one":"a","two":"","three":"c"}

    if v := x["two"]; v == "" { //incorrect
        fmt.Println("no entry")
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Good:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := map[string]string{"one":"a","two":"","three":"c"}

    if _,ok := x["two"]; !ok {
        fmt.Println("no entry")
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="stringsareimmutable"&gt;Strings Are Immutable&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Trying to update an individual character in a string using the index operator will result in a failure. Strings are read-only byte slices (with a few extra properties). If you do need to update a string then use a byte slice instead converting it to a string type when necessary.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := "text"
    x[0] = 'T'

    fmt.Println(x)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Error:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox305565531/main.go:7: cannot assign to x[0]
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := "text"
    xbytes := []byte(x)
    xbytes[0] = 'T'

    fmt.Println(string(xbytes)) //prints Text
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="conversionsbetweenstringsandbyteslices"&gt;Conversions Between Strings and Byte Slices&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;When you convert a string to a byte slice (and vice versa) you get a complete copy of the orginal data. It's not like a cast in other languages and it's not like reslicing where the new slice variable points to the same underlying array used by the original byte slice.&lt;/p&gt;

&lt;h6 id="stringsandindexoperator"&gt;Strings And Index Operator&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The index operator on a string returns a byte, not a character (like it's done in other languages).&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    x := "text"
    fmt.Println(x[0]) //print 116
    fmt.Printf("%T",x[0]) //prints uint8
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you need to access specific string characters use the "for range" clause. The official "unicode/utf8" package and the experimental utf8string package (golang.org/x/exp/utf8string) are also useful. The utf8string package includes a convinient "At" method. Converting the string to a slice of runes is an option too.&lt;/p&gt;

&lt;h6 id="stringsarenotalwaysutf8text"&gt;Strings Are Not Always UTF8 Text&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;String values are not required to be UTF8 text. They can contain arbitrary bytes. The only time strings are UTF8 is when string literals are used. Even then they can include other data using escape sequences.&lt;/p&gt;

&lt;h6 id="missingcommainmultilineslicearraymapliterals"&gt;Missing Comma In Multi-Line Slice/Array/Map Literals&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    x := []int{
    1,
    2 //error
    }
    _ = x
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Errors:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox367520156/main.go:6: syntax error: need trailing comma before newline in composite literal
  /tmp/sandbox367520156/main.go:8: non-declaration statement outside function body
  /tmp/sandbox367520156/main.go:9: syntax error: unexpected }
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

func main() {  
    x := []int{
    1,
    2,
    }
    x = x

    y := []int{3,4,} //no error
    y = y
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You won't get a compile error if you leave the trailing comma when you collapse the declaration to be on a single line.&lt;/p&gt;

&lt;h6 id="logfatalandlogpanicdomorethanlog"&gt;log.Fatal and log.Panic Do More Than Log&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Logging libraries often provide different log levels. Unlike those logging libraries the log package in Go does more then log if you call its Fatal and Panic functions. When your app call those functions Go will also terminate you app :-)&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "log"

func main() {  
    log.Fatalln("Fatal Level: log entry") //app exits here
    log.Println("Normal Level: log entry")
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="builtindatastructureoperationsarenotsynchronized"&gt;Built-in Data Structure Operations Are Not Synchronized&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Even though Go has a number of features to support concurrenty natively concurrency safe data collections are not one them :-) It's your responsibility to ensure the data collection updates are atomic. Goroutines and channels are the recommended way to implement those atomic operations, but you can also leverage the "sync" package if it makes sense for your application.&lt;/p&gt;

&lt;h6 id="iterationvaluesforstringsinrangeclauses"&gt;Iteration Values For Strings in "range" Clauses&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The index value (the first value returned by the "range" operation) is the index of the first byte for the current character (aka Unicode code point, "rune") returned in the second value. It's not the index for the current character like it's done in other languages.&lt;/p&gt;

&lt;p&gt;Don't use "for range" clauses if you use strings as read only byte slices with arbitrary data because you'll get a lot of 0xfffd runes (aka Unicode replacement characters) and not the actual data in those strings.&lt;/p&gt;

&lt;h6 id="iteratingthroughamapusingaforrangeclause"&gt;Iterating Through a Map Using a "for range" Clause&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This is a gotcha if you expect the items to be in a certain order (e.g., ordered by the key value). Each map iteration will produce different results. The Go runtime tries to go an extra mile randomizing the iteration order, but it doesn't always succeed so you may get several identical map iterations. Don't be surprised to see 5 identical iterations in a row.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    m := map[string]int{"one":1,"two":2,"three":3,"four":4}
    for k,v := range m {
        fmt.Println(k,v)
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;And if you use the Go Playground (&lt;a href="https://play.golang.org/"&gt;https://play.golang.org/&lt;/a&gt;) you'll always get the same results because it doesn't recompile the code unless you make a change.&lt;/p&gt;

&lt;h6 id="incrementsanddecrements"&gt;Increments and Decrements&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Many languages have increment and decrement operators. Unlike other languages Go doesn't support the prefix version of the operations. You also can't use the operators in expressions.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    data := []int{1,2,3}
    i := 0
    ++i //error
    fmt.Println(data[i++]) //error
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Errors:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox101231828/main.go:8: syntax error: unexpected ++
  /tmp/sandbox101231828/main.go:9: syntax error: unexpected ++, expecting :
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    data := []int{1,2,3}
    i := 0
    i++
    fmt.Println(data[i])
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="bitwisenotoperator"&gt;Bitwise NOT Operator&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Many languages use &lt;code&gt;~&lt;/code&gt; as the unary NOT operator (aka bitwise complement), but Go reuses the XOR operator (&lt;code&gt;^&lt;/code&gt;) for that.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    fmt.Println(~2) //error
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Error:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox965529189/main.go:6: the bitwise complement operator is ^
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    var d uint8 = 2
    fmt.Printf("%08b\n",^d)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Go still uses &lt;code&gt;^&lt;/code&gt; as the XOR operator, which maybe confusing for some people.&lt;/p&gt;

&lt;p&gt;If you want you can represet an unary NOT operation (e.g, &lt;code&gt;NOT 0x02&lt;/code&gt;) with a binary XOR operation (e.g., &lt;code&gt;0x02 XOR 0xff&lt;/code&gt;). This could explain why &lt;code&gt;^&lt;/code&gt; was chosen.&lt;/p&gt;

&lt;p&gt;Go also has a special 'AND NOT' bitwise operator (&lt;code&gt;&amp;amp;^&lt;/code&gt;), which adds to the NOT operator confusion. It looks like a special feature/hack to support &lt;code&gt;A AND (NOT B)&lt;/code&gt; without requiring parentheses.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    var a uint8 = 0x82
    var b uint8 = 0x02
    fmt.Printf("%08b [A]\n",a)
    fmt.Printf("%08b [B]\n",b)

    fmt.Printf("%08b (NOT B)\n",^b)
    fmt.Printf("%08b ^ %08b = %08b [B XOR 0xff]\n",b,0xff,b ^ 0xff)

    fmt.Printf("%08b ^ %08b = %08b [A XOR B]\n",a,b,a ^ b)
    fmt.Printf("%08b &amp;amp; %08b = %08b [A AND B]\n",a,b,a &amp;amp; b)
    fmt.Printf("%08b &amp;amp;^%08b = %08b [A 'AND NOT' B]\n",a,b,a &amp;amp;^ b)
    fmt.Printf("%08b&amp;amp;(^%08b)= %08b [A AND (NOT B)]\n",a,b,a &amp;amp; (^b))
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="unexportedstructurefieldsarenotencoded"&gt;Unexported Structure Fields Are Not Encoded&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The fields starting with lowercase letters will not be encoded (json, xml, gob, etc), so when you decode the structure you'll end up with zero values in those unexported fields.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import (  
    "fmt"
    "encoding/json"
)

type MyData struct {  
    One int
    two string
}

func main() {  
    in := MyData{1,"two"}
    fmt.Printf("%#v\n",in) //prints main.MyData{One:1, two:"two"}

    encoded,_ := json.Marshal(in)
    fmt.Println(string(encoded)) //prints {"One":1}

    var out MyData
    json.Unmarshal(encoded,&amp;amp;out)

    fmt.Printf("%#v\n",out) //prints main.MyData{One:1, two:""}
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="appexitswithactivegoroutines"&gt;App Exits With Active Goroutines&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The app will not wait for all your goroutines to complete. This is a common mistake for beginners in general. Everybody starts somewhere, so there's no shame in making rookie mistakes :-)   &lt;/p&gt;

&lt;h6 id="sendingtoanunbufferedchannelreturnsassoonasthetargetreceiverisready"&gt;Sending to an Unbuffered Channel Returns As Soon As the Target Receiver Is Ready&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The sender will not be blocked until your message is processed by the receiver. Depending on the machine where you are running the code the receiver goroutine may or may not have enough time to process the message.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    ch := make(chan string)

    go func() {
        for m := range ch {
            fmt.Println("processed:",m)
        }
    }()

    ch &amp;lt;- "cmd.1"
    ch &amp;lt;- "cmd.2" //won't be processed
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="methodswithvaluereceiverscantchangetheoriginalvalue"&gt;Methods with Value Receivers Can't Change the Original Value&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: beginner&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Method receivers are like regular function arguments. If it's declared to be a value then your function/method gets a copy of your receiver argument. This means making changes to the receiver will not affect the original value unless your receiver is a map or a slice and you are updating the items in the collection or the fields you are updating in the receiver are pointers. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

type data struct {  
    num int
    key *string
    items map[string]bool
}

func (this *data) pmethod() {  
    this.num = 7
}

func (this data) vmethod() {  
    this.num = 8
    *this.key = "v.key"
    this.items["vmethod"] = true
}

func main() {  
    key := "key.1"
    d := data{1,&amp;amp;key,make(map[string]bool)}

    fmt.Printf("num=%v key=%v items=%v\n",d.num,*d.key,d.items)
    //prints num=1 key=key.1 items=map[]

    d.pmethod()
    fmt.Printf("num=%v key=%v items=%v\n",d.num,*d.key,d.items) 
    //prints num=7 key=key.1 items=map[]

    d.vmethod()
    fmt.Printf("num=%v key=%v items=%v\n",d.num,*d.key,d.items)
    //prints num=7 key=v.key items=map[vmethod:true]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="updatingandreferencingitemvaluesinslicearrayandmaprangeclauses"&gt;Updating and Referencing Item Values in Slice, Array, and Map "range" Clauses&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The item data value generated in the "range" clause is a copy of the actual item. It's not a reference to the original item. This means that updating the value will not change the original data item. It also means that taking the address of the value will not give you a pointer to the original data item.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    data := []int{1,2,3}
    for _,v := range data {
        v *= 10 //original item is not changed
    }

    fmt.Println("data:",data) //prints data: [1 2 3]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you need to update the original collection record value use the index operator to access the original item.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    data := []int{1,2,3}
    for i,_ := range data {
        data[i] *= 10
    }

    fmt.Println("data:",data) //prints data: [10 20 30]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If your collection holds pointer values then the rules are slightly different. You still need to use the index operator if you want the original record to point to another value, but you can update the data stored at the target location using the second value in the "for range" clause.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    data := []*struct{num int} {{1},{2},{3}}

    for _,v := range data {
        v.num *= 10
    }

    fmt.Println(data[0],data[1],data[2]) //prints &amp;amp;{10} &amp;amp;{20} &amp;amp;{30}
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="slicesreferencinglargedataarrays"&gt;Slices Referencing Large Data Arrays&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;When you reslice a slice the new slice will reference the array of the original slice. If you forget about this behavior it can lead to unexpected memory usage if your application allocates large temporary slices reslicing them to refer to small sections of the original data. To avoid this trap make sure to copy the data from the temporary slide (instead of reslicing it).&lt;/p&gt;

&lt;h6 id="staleslices"&gt;"Stale" Slices&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Multiple slices can reference the same data. This can happen when you reslice a slice, for example. If your application relies on this behavior to function properly then you'll need to worry about "stale" slices. &lt;/p&gt;

&lt;p&gt;When slices grow it's possible for some of these slices to become "stale". Go tries to do as much as it can to help you avoid "stale" slices (refactor this senstence). When an old slice points to the old array and the new slice points to a new reallocated array after new elements were added to it.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;import "fmt"

func main() {  
    s1 := []int{1,2,3}
    fmt.Println(len(s1),cap(s1),s1) //prints 3 3 [1 2 3]

    s2 := s1[1:]
    fmt.Println(len(s2),cap(s2),s2) //prints 2 2 [2 3]

    for i := range s2 { s2[i] += 20 }

    //still referencing the same array
    fmt.Println(s1) //prints [1 22 23]
    fmt.Println(s2) //prints [22 23]

    s2 = append(s2,4)

    for i := range s2 { s2[i] += 10 }

    //s1 is now "stale"
    fmt.Println(s1) //prints [1 22 23]
    fmt.Println(s2) //prints [32 33 14]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="typedeclarationsandtypemethods"&gt;Type Declarations and Type Methods&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;When you create a type declaration by defining a new type from an existing (non-interface) type you don't inherit the methods defined for that existing type.&lt;/p&gt;

&lt;p&gt;Fails:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "sync"

type myMutex sync.Mutex

func main() {  
    var mtx myMutex
    mtx.Lock() //error
    mtx.Unlock() //error  
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Compile Errors:  &lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;span style="color:#900; font-family: Menlo, Courier New, monospace; font-size: 11pt;"&gt;
  /tmp/sandbox106401185/main.go:9: mtx.Lock undefined (type myMutex has no field or method Lock)
  /tmp/sandbox106401185/main.go:10: mtx.Unlock undefined (type myMutex has no field or method Unlock)
  &lt;/span&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;If you do need the methods from the original type you can define a new struct type embedding the original type as an anonymous field.&lt;/p&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "sync"

type myLocker struct {  
    sync.Mutex
}

func main() {  
    var lock myLocker
    lock.Lock() //ok
    lock.Unlock() //ok
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Interface type declarations also retain their method set.&lt;/p&gt;

&lt;p&gt;Works:  &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "sync"

type myLocker sync.Locker

func main() {  
    var lock myLocker = new(sync.Mutex)
    lock.Lock() //ok
    lock.Unlock() //ok
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h6 id="breakingoutofforswitchandforselectcodeblocks"&gt;Breaking Out of "for switch" and "for select" Code Blocks&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;A "break" statement without a label only gets you out of the inner switch/select block. If using a "return" statement is not an option then defining a label for the outer loop is the next best thing.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import "fmt"

func main() {  
    loop:
        for {
            switch {
            case true:
                fmt.Println("breaking out...")
                break loop
            }
        }

    fmt.Println("out!")
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;A "goto" statement will do the trick too...&lt;/p&gt;

&lt;h6 id="iterationvariablesandclosuresinforstatements"&gt;Iteration Variables and Closures in "for" Statements&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This is the most common gotcha in Go. The iteration variables in "for" statements are re-used in each iteration. This means that each closure (aka function literal) created in your "for" loop will reference the same variable (and they'll get that variable's value at the time those goroutines start executing).&lt;/p&gt;

&lt;p&gt;TODO: add samples&lt;/p&gt;

&lt;h6 id="deferredfunctioncallargumentevaluation"&gt;Deferred Function Call Argument Evaluation&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Arguments for a deferred function call are evaluated when the defer statement is evaluated.&lt;/p&gt;

&lt;p&gt;TODO: add samples&lt;/p&gt;

&lt;h6 id="failedtypeassertions"&gt;Failed Type Assertions&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Failed type assertions return the "zero value" for the target type used in the assertion statement. This can be a gotcha if you assign the type assertion result back to the original variable and continue to use that variable assuming it didn't change (because the assertion failed).&lt;/p&gt;

&lt;p&gt;TODO: add samples&lt;/p&gt;

&lt;h6 id="usingpointerreceivermethodsonvalueinstances"&gt;Using Pointer Receiver Methods On Value Instances&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: advanced&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;It's ok to call a pointer receiver method on a value as long as the value is addressable. In other words, you don't need to have a value receiver version of the method in some cases.&lt;/p&gt;

&lt;p&gt;Not every variable is addressable though. Map elements are not addressable. Variables referenced through interfaces are also not addressable.&lt;/p&gt;

&lt;p&gt;TODO: add samples&lt;/p&gt;

&lt;h6 id="nilinterfacesandnilinterfacesvalues"&gt;"nil" Interfaces and "nil" Interfaces Values&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: advanced&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This is the second most common gotcha in Go.&lt;/p&gt;

&lt;p&gt;Gotcha if you are trying to check if the interface equals to nil.&lt;/p&gt;

&lt;p&gt;TODO: add samples&lt;/p&gt;

&lt;h6 id="stackandheapvariables"&gt;Stack and Heap Variables&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: advanced&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;You don't always know if your variable is allocated on the stack or heap.&lt;/p&gt;

&lt;h6 id="gomaxprocsconcurrencyandparallelism"&gt;GOMAXPROCS, Concurrency, and Parallelism&lt;/h6&gt;

&lt;ul&gt;
&lt;li&gt;level: advanced/intermediate&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;By default, Go uses only one execution context / OS thread (in the current version). This number can be increased by setting GOMAXPROCS.&lt;/p&gt;

&lt;p&gt;There's a common misconception that GOMAXPROCS represents the number of CPUs Go will use to run goroutes. The runtime.GOMAXPROCS function documentation adds more to the confusion though the GOMAXPROCS variable description (&lt;a href="https://golang.org/pkg/runtime/"&gt;https://golang.org/pkg/runtime/&lt;/a&gt;) does a better job talking about OS threads instead.&lt;/p&gt;

&lt;p&gt;You can set GOMAXPROCS to more then the number of your CPUs. The max value for GOMAXPROCS is 256.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package main

import (  
    "fmt"
    "runtime"
)

func main() {  
    fmt.Println(runtime.GOMAXPROCS(-1)) //prints: 1
    fmt.Println(runtime.NumCPU())       //prints: 1 (on play.golang.org)
    runtime.GOMAXPROCS(20)
    fmt.Println(runtime.GOMAXPROCS(-1)) //prints: 20
    runtime.GOMAXPROCS(300)
    fmt.Println(runtime.GOMAXPROCS(-1)) //prints: 256
}
&lt;/code&gt;&lt;/pre&gt;</description><link>http://localhost:2368/gotchas-and-common-mistakes-in-go-golang/</link><guid isPermaLink="false">e80c8648-431a-4964-a8f1-e022da0ab0cc</guid><category>golang</category><dc:creator>Kyle Quest</dc:creator><pubDate>Fri, 10 Apr 2015 15:42:39 GMT</pubDate></item></channel></rss>